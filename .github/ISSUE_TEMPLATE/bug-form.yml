name: Bug Report
description: File a bug report
title: "[Bug]: "
labels: ["bug", "triage"]
assignees:
  - octocat
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to fill out this bug report!
  - type: input
    id: contact
    attributes:
      label: Contact Details
      description: How can we get in touch with you if we need more info?
      placeholder: ex. email@example.com
    validations:
      required: false
  - type: textarea
    id: what-happened
    attributes:
      label: What happened?
      description: Also tell us, what did you expect to happen?
      placeholder: Tell us what you see!
      value: "A bug happened!"
    validations:
      required: true
  - type: dropdown
    id: project
    attributes:
      label: Select Project
      description: Choose the relevant project associated to the issue found
      options:
        - FE/JSE
        - Native
        - Registration
        - Backend
        - Unities
        - OpsSW and Tools
        - Colin
    validations:
      required: true
    if [[ project == "FE/JSE" ]]; then
  - type: checkboxes
    id: choice
    attributes:
      label: Did you choose FE/JSE
      description: Say the truth
      options:
        - label: Yes
        - label: No
    fi
    if [[ project == "Native" ]]; then
  - type: checkboxes
    id: choice
    attributes:
      label: Did you choose Native
      description: Say the truth
      options:
        - label: Yes
        - label: No
    fi
    if [[ project == "Registration" ]]; then
  - type: checkboxes
    id: choice
    attributes:
      label: Did you choose Registration
      description: Say the truth
      options:
        - label: Yes
        - label: No
    fi
    if [[ project == "Backend" ]]; then
  - type: checkboxes
    id: choice
    attributes:
      label: Did you choose Backend
      description: Say the truth
      options:
        - label: Yes
        - label: No
    fi
    if [[ project == "Unities" ]]; then
  - type: checkboxes
    id: choice
    attributes:
      label: Did you choose Unities
      description: Say the truth
      options:
        - label: Yes
        - label: No
    fi
    if [[ project == "OpsSW and Tools" ]]; then
  - type: checkboxes
    id: choice
    attributes:
      label: Did you choose OpsSW and Tools
      description: Say the truth
      options:
        - label: Yes
        - label: No
    fi
    if [[ project == "Colin" ]]; then
  - type: checkboxes
    id: choice
    attributes:
      label: Did you choose Colin
      description: Say the truth
      options:
        - label: Yes
        - label: No
    fi
